html{
    scroll-behavior:smooth;
}

body{
    background-color:121212;
    color:lightgrey;
}

ul{
    list-style-type:none;
    padding:0;
    margin:0;
}

a{
    text-decoration:none;
    color:whitesmoke;
}

#nav-bar{
    float:left;
    display:block;
    font-size:20px;
}

.nav-btn{
    background-color:#ff5733;
    border-radius:5px;
    padding:15px 10px;
}

/*journal page content*/
#ledger-view{
    display:flex;
    flex-direction:column;
    gap:2rem;
}
/*title*/
.heading span{
    font-size:120px;
    color:34D058;
}

#heading-tag{
    color:#9932cc;
}

/* ledger flex box */
.ledger-flex-container{
}

/* create ledger group */
.create-ledger-container{
    display:flex;
    flex-direction:column;
    gap:30px;
    max-width:30%;
    position:relative;
}

.create-ledger-container h2{
    background-color:9932cc;
    border-radius:4px;
    padding:5px 10px;
}

/* ledger btn container */
.btn-group{
    display:flex;
    flex-direction:row;
    justify-content:space-between;
}

/*add ledger btn*/
/*scroll up btn*/
#create-btn,#scroll-up{
    width:fit-content;
    border:none;
    border-radius:5px;
    padding:10px;

    font-family:cursive;
    color:9932cc;
    background-color:333333;
    font-size:20px;
    transition-duration:.5s;
    cursor:pointer;
}

#clear-btn{
    width:fit-content;
    border:none;
    border-radius:5px;
    padding:10px;

    font-family:cursive;
    color:9932cc;
    background-color:333333;
    font-size:20px;
    transition-duration:.5s;
    cursor:pointer;
}

#create-btn:hover,#clear-btn:hover,#scroll-up:hover{
    transition-duration:.5s;
    color:333333;
    background-color:9932cc;
}

/*text boxes*/
input[type='text'],input[type='number']{
    background-color:inherit;
    border-left:none;
    border-right:none;
    border-top:none;
    border-bottom-color:9932cc;
    color:lightgrey;
    font-size:15px;
}

input[type='text'],input[type='number']:focus{
    border-left:none;
    border-right:none;
    border-top:none;
    border-bottom:333333;
    outline:none;
}
/*date box*/
input[type='date']{
    background-color:333333;
    padding:5px;
    border:none;
    border-radius:3px;
    color:lightgrey;
}
